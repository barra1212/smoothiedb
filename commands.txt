// Create requirements.txt file
sudo pip3 freeze --local > requirements.txt

// Create Procfile
echo web: (the filename here) > Procfile

// Push to Heroku
git push heroku master

// Tell Heroku to run the App
heroku ps:scale web=1